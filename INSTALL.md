**Важно:** Файлы весов моделей должны находиться в локальной папке `./weights`, чтобы контейнер мог подхватить их через `volumes`.
## Запуск на Windows

На Windows работа Docker базируется на графическом интерфейсе и подсистеме **WSL2**.

1. **Установка Docker Desktop**: Скачайте и установите его с [официального сайта Docker](https://www.docker.com/products/docker-desktop/).
    
2. **Активация WSL2**: При установке Docker предложит использовать **WSL2 (Windows Subsystem for Linux)**. Необходимо согласиться. Если сервис не установлен в системе, его необходимо установить, инструкция есть на официальном сайте Microsoft https://learn.microsoft.com/en-us/windows/wsl/install. 
    
3. **Запуск сервиса**: Запустите Docker Desktop и дождитесь, пока иконка кита в трее станет зеленой.
    
4. **Запуск проекта**: Откройте терминал, перейдите в папку проекта и выполните:
    
    ```
    docker-compose up --build
    ```

4. **Ожидание**: Первая сборка может занять **10-15 минут**.

Когда всё закончится, в терминале появится надпись: 
``` 
You can now view your Streamlit app in your browser.` `Local URL: http://localhost:8501
```

5. **Доступ**: Откройте браузер и введите [http://localhost:8501](https://www.google.com/search?q=http://localhost:8501).



## 🐧 Запуск на Linux (Ubuntu/Debian)


1. **Установка Docker и Compose**:
    
    Bash
    
    ```
    sudo apt-get update
    sudo apt-get install docker.io docker-compose -y
    ```
    
2. **Настройка прав (опционально)**: Чтобы не вводить `sudo` перед каждой командой, добавьте своего пользователя в группу Docker:
    
    Bash
    
    ```
    sudo usermod -aG docker $USER
    # Чтобы изменения вступили в силу без перезагрузки:
    newgrp docker
    ```
    
3. **Запуск проекта**: Перейдите в папку с проектом и запустите сборку:
    
    Bash
    
    ```
    docker-compose up --build
    ```
4. **Ожидание**: Первая сборка может занять **10-15 минут**.

Когда всё закончится, в терминале появится надпись: 
``` 
You can now view your Streamlit app in your browser.` `Local URL: http://localhost:8501
``` 

5. **Доступ**: Приложение будет доступно по адресу `http://localhost:8501` или по IP-адресу вашего сервера.